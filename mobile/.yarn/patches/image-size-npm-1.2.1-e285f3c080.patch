diff --git a/dist/types/icns.js b/dist/types/icns.js
index f2bfafef3723cb423b110304815e56e3f97f81e0..e93621052bd3dff03ae1383eb882f9ee984eeea3 100644
--- a/dist/types/icns.js
+++ b/dist/types/icns.js
@@ -82,6 +82,9 @@ exports.ICNS = {
         let imageOffset = SIZE_HEADER;
         let imageHeader = readImageHeader(input, imageOffset);
         let imageSize = getImageSize(imageHeader[0]);
+        // Zero/invalid entry length would never advance the offset (DoS).
+        if (!imageHeader[1] || imageHeader[1] <= 0)
+            return imageSize;
         imageOffset += imageHeader[1];
         if (imageOffset === fileLength)
             return imageSize;
@@ -93,6 +96,8 @@ exports.ICNS = {
         while (imageOffset < fileLength && imageOffset < inputLength) {
             imageHeader = readImageHeader(input, imageOffset);
             imageSize = getImageSize(imageHeader[0]);
+            if (!imageHeader[1] || imageHeader[1] <= 0)
+                break;
             imageOffset += imageHeader[1];
             result.images.push(imageSize);
         }
diff --git a/dist/types/jxl.js b/dist/types/jxl.js
index f557b0aea7e679d2cb84d9915e957dd2254eba97..1a721661ea68823b8e35d2aeecf1dabc8d77c265 100644
--- a/dist/types/jxl.js
+++ b/dist/types/jxl.js
@@ -21,10 +21,14 @@ function extractPartialStreams(input) {
     let offset = 0;
     while (offset < input.length) {
         const jxlpBox = (0, utils_1.findBox)(input, 'jxlp', offset);
-        if (!jxlpBox)
+        if (!jxlpBox || jxlpBox.size <= 0)
             break;
         partialStreams.push(input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size));
-        offset = jxlpBox.offset + jxlpBox.size;
+        const nextOffset = jxlpBox.offset + jxlpBox.size;
+        // Guard against non-advancing offsets from malformed boxes.
+        if (nextOffset <= offset)
+            break;
+        offset = nextOffset;
     }
     return partialStreams;
 }
diff --git a/dist/types/utils.js b/dist/types/utils.js
index 5224bbafe87551ac415cb3de234820ccc0ff6e2c..ad19a5a9228b2f8b5d44a2d16ea701efbd69af75 100644
--- a/dist/types/utils.js
+++ b/dist/types/utils.js
@@ -65,9 +65,10 @@ function findBox(input, boxName, offset) {
         const box = readBox(input, offset);
         if (!box)
             break;
-        if (box.name === boxName)
+        // Skip zero-sized boxes: returning them lets callers loop forever
+        // when they advance with `offset + box.size`.
+        if (box.size > 0 && box.name === boxName)
             return box;
-        // Fix the infinite loop by ensuring offset always increases
         // If box.size is 0, advance by at least 8 bytes (the size of the box header)
         offset += box.size > 0 ? box.size : 8;
     }
